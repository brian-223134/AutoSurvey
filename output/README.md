# 생성된 서베이 — 결과

각 디렉터리에 어떤 결과물이 있고, 볼 때 무엇을 주의해야 하는지 정리한 문서입니다.

**실행 조건(모델·하이퍼파라미터·환경·명령)은 [`../REPRODUCTION.md`](../REPRODUCTION.md)에 있습니다.**
새 서베이를 만들면 아래 결과 표와 `REPRODUCTION.md` §5-2 조건 표에 각각 한 줄씩 추가해 주세요.

---

## 결과

| 디렉터리 / 토픽 | 모델 | DB | 섹션/서브 | 단어 | 참고문헌 | 쪽 | 비용 |
|---|---|---|---|---|---|---|---|
| `haiku/` In-context Learning | claude-3-haiku | 배포본 | 9 / 51 | 28,161 | 383 | 74 | $0.78 |
| `haiku/` Large Multi-Modal LM | claude-3-haiku | 배포본 | 8 / 48 | 26,410 | 378 | 72 | $0.75 |
| `haiku/` Evaluation of LLMs | claude-3-haiku | 배포본 | 8 / 48 | 26,294 | 368 | 71 | $0.75 |
| `deepseek-v4-pro/` In-context Learning | deepseek-v4-pro | 배포본 | 10 / 72 | 86,121 | 644 | 184 | $3.39 |
| `deepseek-v4-flash/` RAG for LLMs | v4-flash-0731 | 배포본 | 10 / 39 | 49,002 | 476 | 111 | $0.376 |
| **`deepseek-v4-flash-newdb/` RAG for LLMs** | v4-flash-0731 | **최신화본** | 11 / 44 | 56,273 | 632 | 137 | $0.420 |
| **`deepseek-v4-flash-newdb/` 3D Gaussian Splatting** | v4-flash-0731 | **최신화본** | 12 / 48 | 59,980 | 804 | 153 | $0.456 |
| **`deepseek-v4-flash-newdb/` LLM-based Multi-Agent** | v4-flash-0731 | **최신화본** | 7 / 28 | 40,599 | 486 | 103 | $0.286 |

<sub>**기준 통일 (2026-08-18)**: 섹션·서브섹션은 **번호 중복 헤딩을 제거한** 값
(모델이 본문에 제목을 다시 쓰는 일이 있어 `.md` 의 `##` 를 그대로 세면 부풀려집니다).
단어는 **인용 표기 `[1; 2]` 를 뺀** 값. 쪽수는 `.tex` 를 pdflatex 으로 컴파일해
`pdfinfo` 로 센 실측값(목차·참고문헌 포함, 약 457단어/쪽).
예전 표는 haiku·v4-pro 행만 `check_survey.py` 원값이라 기준이 섞여 있었습니다.</sub>

**본편 8편 전부 무결성 검사 통과**(댕글링 인용 0, json 매핑 일치)이고
**전부 PDF 로 컴파일됩니다**(에러 0, 미해결 인용 0).

**스모크 3편은 이 표에서 뺐습니다** — 축소 설정(`section 4 / ref 200 / rag 15`)으로
파이프라인이 끝까지 도는지만 확인한 것이라 품질 비교 대상이 아닙니다.
다만 방법론 근거로는 쓰입니다(아래 "스모크가 알려준 것").

| 스모크 | 모델 | 섹션/서브 | 단어 | 참고문헌 | 쪽 | 비용 |
|---|---|---|---|---|---|---|
| `haiku-smoke/` In-context Learning | claude-3-haiku | 5 / 18 | 10,469 | 107 | 26 | $0.15 |
| `deepseek-smoke/` In-context Learning | deepseek-v4-pro | 5 / 29 | 31,160 | 190 | 67 | $1.35 |
| `deepseek-v4-flash-smoke/` RAG for LLMs | v4-flash-0731 | 5 / 37 | 44,803 | 268 | 93 | $0.166 |

**스모크가 알려준 것** — 결과물이 아니라 방법론 근거로 인용하세요.

- **`--rag_num` 은 분량 인자가 아니라 비용 인자다.** `deepseek-v4-flash-smoke/`(rag 15)와
  `deepseek-v4-flash/`(rag 60)는 검색량이 4배 차이인데 서브섹션당 단어는
  1,211 → 1,256 으로 **4% 늘었을 뿐**이고 비용은 2.3배가 됐습니다.
  본편끼리는 `rag_num` 이 전부 60 이라 이 대조가 만들어지지 않습니다.
- **`deepseek-smoke/` 는 추론이 켜진 채 돌았습니다.** 비용이 토큰 추정의 3.5배로 튀었고
  (추론 토큰이 출력의 45%), 그래서 `AUTOSURVEY_REASONING` 토글을 만들었습니다.
  본편과 설정이 다르므로 나란히 놓지 마세요.

### 배포본 vs 최신화본 — 같은 토픽, DB 만 다름

`--db_path` 하나만 바꾸고 나머지(모델·provider·토픽·`section 8 / subsec 4 / len 700 /
rag 60 / ref 1200`)를 전부 맞춘 통제 비교입니다.

| | A `deepseek-v4-flash/` | B `deepseek-v4-flash-newdb/` |
|---|---|---|
| DB | 배포본 537,665편 | **최신화본 909,293편** |
| 참고문헌 | 476 | **632** |
| 가장 최신 인용 | 2024-04 | **2026-08** |
| **2024-07 이후 비중** | **0.0%** | **89.1%** |
| 구조 | 10섹션 / 39서브 | 11섹션 / 44서브 |
| 단어 / 쪽 | 49,002 / 111p | 56,273 / 137p |
| 분량 계수 | 1.79x | 1.83x |
| 비용 / 재시도 | $0.376 / 4회 | $0.420 / 3회 |
| 무결성 | OK | OK |

**DB 최신화가 실제로 작동합니다** — B 인용의 89%가 배포본에는 존재조차 하지 않던
논문입니다. 분량 계수는 1.79 → 1.83 으로 거의 그대로라, DB 를 바꿔도 모델의
분량 성향은 변하지 않는다는 것도 확인됐습니다.

**⚠ 그런데 SurveyBench 로는 이 개선을 잴 수 없습니다. 오히려 점수가 착시로 오릅니다.**

| | 인용 | **채점 분모** | **적중** | coverage |
|---|---|---|---|---|
| A (배포본) | 476 | 476 | **48** | 0.101 |
| B (최신화본) | 632 | **69** | **9** | **0.130** |

채점기가 정답셋 기준일(2024-07)보다 새 인용을 분모에서 뺍니다. B 는 632편 중
**563편(89%)이 제외**돼 분모가 69로 무너졌습니다. **coverage 는 0.101 → 0.130 으로
올랐지만 실제 적중은 48 → 9편으로 5분의 1입니다.**
비율이 아니라 **적중 편수**를 봐야 합니다.

> 같은 현상이 SurveyForge 쪽 회귀 검사에도 있습니다(구 DB 38편 → 신 DB 18편,
> coverage 0.304 → 0.643). 우리 쪽이 더 극단적인 것은 인용 수가 많기 때문입니다.

### 최신화본 × 토픽 3편 — `deepseek-v4-flash-newdb/`

같은 파라미터로 **토픽만** 바꾼 실행입니다(전부 최신화본 DB, SurveyBench 토픽 문자열 그대로).

| 토픽 | 섹션/서브 | 단어 | 서브당 | **계수** | 참고문헌 | 쪽 | 2024-07 이후 | 비용 | 소요 |
|---|---|---|---|---|---|---|---|---|---|
| RAG for LLMs | 11 / 44 | 56,273 | 1,279 | 1.83× | 632 | 137 | **89.1%** | $0.420 | 약 20분 |
| 3D Gaussian Splatting | 12 / 48 | 59,980 | 1,250 | 1.79× | 804 | 153 | **82.1%** | $0.456 | 17분 24초 |
| LLM-based Multi-Agent | **7** / 28 | 40,599 | 1,450 | **2.07×** | 486 | 103 | **92.2%** | $0.286 | 20분 19초 |

**최신화 DB 가 실제로 쓰입니다** — 세 토픽 모두 인용의 **82~92% 가 2024-07 이후**입니다.
배포본으로 만든 산출물은 이 비율이 전부 0.0% 입니다.

#### 통제되는 것과 안 되는 것

| 인자 | 지시 | 실제 | 판정 |
|---|---|---|---|
| `--subsection_num 4` | 4 | 4.0 / 4.0 / 4.0 | ✅ **정확히 지켜짐** |
| `--section_num 8` | 8 | **11 / 12 / 7** | ❌ **7~12 로 흔들림** |

**`--section_num` 은 초과만 하는 게 아니라 미달도 합니다.** Multi-Agent 는 8을 줬는데
7이 나왔습니다. 배율로 보면 0.88~1.5배이고 **방향조차 예측되지 않습니다.**
같은 파라미터로 총 분량이 40,599~59,980 단어(103~153쪽)로 벌어지는 주된 원인입니다.

> 앞서 "네 실행 모두 정확히 1.25배 초과" 로 적었던 것은 표본이 셋일 때의 관찰이었고,
> Multi-Agent 로 **반증됐습니다.**

**분량 계수는 반대로 안정적입니다** — 1.79 / 1.83 / 1.79 / 2.07 (본편 v4-flash 4편).
Multi-Agent 의 2.07 은 서브섹션이 28개로 적어 **한 편당 더 길게 쓴 보상 효과**로 보입니다
(서브섹션 수와 서브섹션당 길이가 반대로 움직임).

#### SurveyBench 채점 — 참고용일 뿐입니다

| 토픽 | 인용 | 채점 분모 | **적중** | coverage | 정답셋 |
|---|---|---|---|---|---|
| RAG | 632 | 69 (**89% 제외**) | **9** | 0.130 | 608편 / 2024-07 |
| 3D Gaussian Splatting | 804 | 132 (**84% 제외**) | **33** | 0.250 | 330편 / 2024-06 |
| LLM-based Multi-Agent | 486 | 38 (**92% 제외**) | **1** | 0.026 | 823편 / 2024-07 |

> **이 수치로 시스템을 평가하면 안 됩니다.** 세 토픽 모두 인용의 84~92% 가 정답셋
> 기준일 이후라 분모에서 빠졌습니다. Multi-Agent 는 486편 중 38편만 채점돼
> **적중 1편**이 되었는데, 이건 품질이 아니라 표본이 무너진 결과입니다.
> 배포본으로 만든 본편 A(RAG)만 공정하게 비교됩니다 — 인용 476편 전부가 채점돼
> 적중 48편 / coverage 0.101 입니다.

**참고문헌이 인간 서베이보다 훨씬 많습니다.**

| 토픽 | 우리 | 인간 서베이 | 배수 |
|---|---|---|---|
| RAG | 632 | 191 | 3.3배 |
| 3D Gaussian Splatting | 804 | 271 | 3.0배 |
| LLM-based Multi-Agent | 486 | 173 | 2.8배 |

가드(400편)를 두 편이 넘습니다. **많이 인용하는 것이 품질이 아니라는 근거**로 쓰세요.

**쪽수는 실측입니다** — `.tex` 를 pdflatex 으로 컴파일해 `pdfinfo` 로 셌습니다
(2026-08-18, 8편 전부 에러 0 / 미해결 인용 0). 우리 템플릿은 **약 457단어/쪽**이고
목차·참고문헌이 포함됩니다. 컴파일 절차는 `../README.md` §6.

`scripts/check_survey.py` 기준입니다(8편 전부 통과 — 댕글링 인용 0, json 매핑 일치).
v4-flash 두 편의 섹션/서브섹션은 **중복 헤딩 제거 후** 기준이고, 단어 수는 인용 표기를
뺀 값입니다(`check_survey.py` 원 출력은 각각 42/51 서브섹션, 47,666/54,004 단어).

**인용 커버리지 채점 결과 (2026-08-18)** — RAG 토픽만. 상세는 `../docs/evaluation-note.md`.

| 산출물 | 인용 | 적중 | coverage(정밀도) | 재현율 |
|---|---|---|---|---|
| 인간 작성 | 191 | 103 | **0.539** | **0.169** |
| SurveyForge (저자) | 85 | 37 | 0.435 | 0.061 |
| `deepseek-v4-flash/` | 476 | 48 | **0.101** | 0.079 |
| `deepseek-v4-flash-smoke/` | 268 | 22 | 0.082 | 0.036 |

> **coverage 는 정밀도라 많이 인용할수록 내려갑니다.** 재현율로는 SurveyForge 보다
> 높습니다. 참고문헌 476편 중 제목에 검색/RAG 관련어가 있는 건 28% 뿐이고, 벗어난
> 것들이 hallucination 에 몰려 있습니다 — 검색이 인접 주제로 샜습니다.

> ⚠ **deepseek 편의 섹션 수는 이 표 기준으로는 부풀려져 있습니다.**
> `check_survey.py`는 `.md`의 `##` / `###` 를 그대로 세는데, deepseek는
> 서브섹션을 `## 2.5` 로 쓰거나 제목을 두 번 쓴 곳이 있습니다.
> 중복을 제거하고 번호 깊이로 레벨을 다시 매긴 `.tex` 기준 실제 구조는
> **10섹션 / 72서브섹션 / 5서브서브섹션**, `deepseek-smoke/`는 **5 / 29** 입니다.
> haiku 편은 두 기준이 일치합니다.

---

## 비교할 때 주의할 점

**haiku 3편끼리만 통제된 비교가 됩니다.** 나머지는 조건이 겹치지 않습니다.

- **`deepseek-v4-pro/` vs `haiku/` (같은 토픽)** — `rag_num`이 30 대 60으로 다릅니다.
  모델 차이만 보는 비교가 아닙니다. 그런데도 deepseek 쪽이 분량 2.9배,
  참고문헌 1.7배였습니다.
- **스모크 2편은 품질 비교용이 아닙니다.** 축소 설정(`section_num 4` /
  `outline_reference_num 200` / `rag_num 15`)으로 파이프라인이 끝까지 도는지만
  확인한 것입니다. 본편과 나란히 놓고 평가하지 마세요.
- **`deepseek-smoke/`는 추론이 켜진 채로 돌았습니다.** `AUTOSURVEY_REASONING`
  토글이 생기기 전 실행이라 본편(추론 OFF)과 설정이 다릅니다. 스모크 비용이
  토큰 추정의 3.5배로 튄 원인이기도 합니다(추론 토큰이 출력의 45%).
- **`deepseek-smoke/`는 두 번째 시도 결과입니다.** 첫 시도는 아웃라인 파서가
  `IndexError`로 죽었습니다(`8bfbc43`).
- **haiku는 추론 모델이 아니므로** 추론 설정이 결과에 영향을 주지 않습니다.
- **논문 수치와는 비교할 수 없습니다.** 논문은 전문(full-text) DB를 쓰는데
  이 DB에는 초록만 있습니다. `../SETTING.md` §8.

---

## 산출물 형식

디렉터리마다 토픽당 3개 파일이 있습니다.

| 파일 | 내용 |
|---|---|
| `{topic}.md` | 본문 + `## References`(번호와 제목) |
| `{topic}.json` | `survey` / `reference`(번호→arXiv id) / `reference_detail`(id·제목·날짜·링크) |
| `{topic}.tex` | Overleaf용. `scripts/md_to_tex.py`가 생성 |

- `reference` 필드는 `src/agents/judge.py`의 `citation_quality()`가 쓰는 구조라
  **바꾸면 `evaluation.py`가 깨집니다.** 서지정보는 `reference_detail`에 있습니다.
- `.tex`는 `.md`에서 만들되 제목 중복 제거, 헤딩 레벨 교정, `\cite` 변환,
  pdflatex 미지원 유니코드 치환을 거칩니다. 자세한 내용은 `scripts/md_to_tex.py`.
- 서버에서는 컴파일이 불가능합니다(TeX 매크로 트리 없음). Overleaf에서 확인하세요.

### Overleaf에 올리기

Overleaf → New Project → Blank Project → `.tex` 업로드 → main document 지정 → Recompile.
여러 편을 한 번에 보려면 `output/`을 zip으로 묶어 **Upload Project**.

> deepseek 편은 92,707단어 + 참고문헌 644개라 무료 Overleaf 제한시간(약 20초)을
> 넘길 수 있습니다. 걸리면 `\tableofcontents` 주석 처리부터 시도하세요.

---

## 재실행 / 재변환

실행 명령과 후처리 절차는 [`../REPRODUCTION.md`](../REPRODUCTION.md) §5-3, §7에 있습니다.
